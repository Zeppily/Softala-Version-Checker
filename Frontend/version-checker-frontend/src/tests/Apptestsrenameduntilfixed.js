import React from 'react';
import { render, screen } from '@testing-library/react';
import App from '../App.js';

/*test('renders app component banner', () => {
  render(<App />);
  const textElement = screen.getByText("Version checker");
  expect(textElement).toBeInTheDocument();
});

test('renders app component update button', () => {
  render(<App />);
  const buttonElement = screen.getByText("Update forms");
  expect(buttonElement).toBeInTheDocument();
});*/
